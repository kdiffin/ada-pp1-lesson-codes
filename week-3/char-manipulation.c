#include <stdio.h>

int main() {
    char grade = 'A';
    char gr = 'y';
    char digit = '5';

    //printf("%d\n", digit); - This would give a error because digit is a character, not an integer
    printf("%c\n", digit);
    return 0;
}